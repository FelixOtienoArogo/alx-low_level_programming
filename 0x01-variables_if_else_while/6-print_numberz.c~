#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */

/* betty style doc for function main goes there */
int main(void)
{
	int ch;
	char end = '\n';

	for (ch = 0 ; ch <= 9 ; ch++)
		{putchar (ch); }
	putchar(end);

	return (0);
}

