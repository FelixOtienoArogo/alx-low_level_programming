#include<stdio.h>
#include<string.h>
#include "main.h"

/**
* factorial - this function returns the factorial of a given number
* @n: the number
*
* Return: length of the string.
*/

int factorial(int n)
{
int a;
a = 1;

if (n > 0)
a = n * factorial(n - 1);

return (a);
}
