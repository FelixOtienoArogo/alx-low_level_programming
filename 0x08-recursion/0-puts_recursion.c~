#include<stdio.h>
#include<string.h>

/**
* _puts_recursion - this function prints a string
* @s: a pointer
*
* Return: nothing.
*/
void _puts_recursion(char *s)
{
int a;
a = strlen(s);
if (*s != '\0' && a != 0)
{
printf("%c", *(s));
_puts_recursion(s + 1);
}
else if (*s == '\0')
printf("\n");
}
